<!DOCTYPE html>
<html>
<head>
  <title>Ticketer</title>

  <%  if request.env['HTTP_USER_AGENT'] =~ /MSIE/ %>
    <%= stylesheet_link_tag :defaults_ie, :forms %>
  <% elsif %>
    <%= stylesheet_link_tag :defaults, :forms, 'menubox' %>
  <% end %>

  <%= javascript_include_tag :defaults, 'menubox' %>
  <%= csrf_meta_tag %>
</head>
<body>
    <div id="page">
      <div class="header">
        <h1 class="logo">Ticketer</h1>
      </div>
      <div id="main">
        <div class="login-holder">
          <div class="login infos">
             <h2 class="user"><%= link_to "Hospital Santa Izabel", main_path %></h2>
          </div>
          <div class="triangle"></div>
        </div>
        <%= yield %>
      </div>
    <div class="footer">
    </div>
    <script type="text/javascript">
      $(document).ready(function(){
        $('.cadastro').menubox('.menu-cadastro')
      });
    </script>
  </body>
</html>
