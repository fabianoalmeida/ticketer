<div class="form">
  <fieldset>
    <legend><%= t('model.local.editing') %></legend>
    <%= render :partial => 'form', :locals => {:disabled => false } %>
  </fieldset>
</div>
<div class="buttons-nav">
  <%= link_to t('model.show'), @local , :class => "button medium" %> 
  <%= link_to t('model.back'), locals_path , :class => "button medium" %>
</div>
