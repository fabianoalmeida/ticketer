<h1>Listing panels</h1>

<table>
  <tr>
    <th>Value</th>
    <th>Ip</th>
    <th>Status</th>
    <th>User</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @panels.each do |panel| %>
  <tr>
    <td><%= panel.value %></td>
    <td><%= panel.ip %></td>
    <td><%= panel.status.value %></td>
    <td><%= panel.user %></td>
    <td><%= link_to 'Show', place_panel_path(panel.place, panel) %></td>
    <td><%= link_to 'Edit', edit_place_panel_path(panel.place, panel) %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Panel', new_place_panel_path(@place) %>
