<h1><%= t('model.panel.title') %></h1>

<table>
  <tr>
    <th><%= t('model.value') %></th>
    <th><%= t('model.ip') %></th>
    <th><%= t('model.status_des') %></th>
    <th><%= t('model.user') %></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @panels.each do |panel| %>
  <tr>
    <td><%= panel.value %></td>
    <td><%= panel.ip %></td>
    <td><%= panel.status.value %></td>
    <td><%= panel.user %></td>
    <td><%= link_to t('model.show'), place_panel_path(panel.place, panel) %></td>
    <td><%= link_to t('model.edit'), edit_place_panel_path(panel.place, panel) %></td>
    <td><%= link_to t('model.panel.tickets'), place_panel_tickets_path(panel.place, panel.id) %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to t('model.panel.new'), new_place_panel_path(@place) %> |
<%= link_to t('model.place.back'), places_path %>
