<h1>Listing places</h1>

<table>
  <tr>
    <th>Value</th>
    <th>User</th>
    <th>Status</th>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @places.each do |place| %>
  <tr>
    <td><%= place.value %></td>
    <td><%= place.user %></td>
    <td><%= place.status.value %></td>
    <td><%= link_to 'Show', place %></td>
    <td><%= link_to 'Edit', edit_place_path(place) %></td>
    <td><%= link_to 'Totems', place_totems_path(place) %></td>
    <td><%= link_to 'Wickets', place_wickets_path(place) %></td>
    <td><%= link_to 'Panels', place_panels_path(place) %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Place', new_place_path %>
