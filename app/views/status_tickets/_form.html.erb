<%= form_for(@status_ticket) do |f| %>
  <% if @status_ticket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@status_ticket.errors.count, "error") %> prohibited this status_ticket from being saved:</h2>

      <ul>
      <% @status_ticket.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label t('model.value') %><br />
    <%= f.text_field :value, :maxlength => 80, :size => 30 %>
  </div>
  <div class="field">
    <%= f.label t('model.acronym') %><br />
    <%= f.text_field :acronym, :maxlength => 3 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
