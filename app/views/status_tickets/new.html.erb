<div class="form">
  <fieldset>
    <legend><%= t('model.status_ticket.new') %></legend>
    <%= render partial: 'form', locals: { disabled: false } %>
  </fieldset>
</div>
<div class="buttons-nav">
  <%= link_to t('model.back'), status_tickets_path , class: "button medium"%>
</div>
