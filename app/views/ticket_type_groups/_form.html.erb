<%= form_for(@ticket_type_group) do |f| %>
  <% if @ticket_type_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ticket_type_group.errors.count, "error") %> prohibited this ticket_type_group from being saved:</h2>

      <ul>
      <% @ticket_type_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :value %><br />
    <%= f.text_field :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
