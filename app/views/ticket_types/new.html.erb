<div class="form">
  <fieldset>
    <legend><%= t('model.ticket_type.new') %></legend>
    <%= render :partial => 'form', :locals => {:disabled => false } %>
  </fieldset>
</div>
<div class="buttons-nav">
  <%= link_to t('model.back'), ticket_type_group_ticket_types_path(@ticket_type_group), :class => "button medium" %>
</div>
