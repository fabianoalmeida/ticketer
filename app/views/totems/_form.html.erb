<%= form_for(@totem) do |f| %>
  <% if @totem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@totem.errors.count, "error") %> prohibited this totem from being saved:</h2>

      <ul>
      <% @totem.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :value %><br />
    <%= f.text_field :value %>
  </div>
  <div class="field">
    <%= f.label :ip %><br />
    <%= f.text_field :ip %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
