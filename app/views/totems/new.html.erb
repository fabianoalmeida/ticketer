<div class="form">
  <fieldset>
    <legend><%= t('model.totem.new') %></legend>
    <%= render :partial => 'form', :locals => {:disabled => false } %>
  </fieldset>
</div>
<div class="buttons-nav">
  <%= link_to t('model.back'), place_totems_path(@place), :class => "button medium"  %>
</div>
